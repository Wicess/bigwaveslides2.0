/** Fired on the window whenever the cart changes, so the header badge can refresh. */
export const CART_CHANGED_EVENT = "bws:cart-change";

/** Fired to open the slide-in cart drawer (e.g. after "Add to cart"). */
export const OPEN_CART_EVENT = "bws:open-cart";
